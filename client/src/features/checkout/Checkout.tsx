import { Typography } from "@mui/material";
import React from "react";

const Checkout = () => {
  return (
    <Typography>only logged in users should be able to see this!</Typography>
  );
};

export default Checkout;
